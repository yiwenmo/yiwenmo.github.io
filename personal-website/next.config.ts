import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/yiwenmo.github.io',
  assetPrefix: '/yiwenmo.github.io',
};

export default nextConfig;
